# Local config copied from .env.example — may contain the admin password.
# (.env.example is committed; .env is not.)
.env

# Third-party mod binaries dropped into a game's mods/ folder.
# Delete this line if you'd rather version your mod set.
mods/*.pak

# Editor / OS noise
.DS_Store
Thumbs.db
*~
*.swp
.idea/
.vscode/

# Logs and local scratch
*.log
/tmp/
/scratch/

# Never commit runtime server data if an install points at the repo
/steamcmd/
/server/
/backups/
