# Local config copied from .env.example — may contain the admin password.
# (.env.example is committed; .env is not.)
.env

# Editor / OS noise
.DS_Store
Thumbs.db
*~
*.swp
.idea/
.vscode/

# Logs and local scratch
*.log
/tmp/
/scratch/

# Never commit runtime server data if an install points at the repo
/steamcmd/
/server/
/backups/
